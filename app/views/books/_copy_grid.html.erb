<ul class="books books_grid">
  <% copies.each do |copy| %><li class="<%= copy.available? ? "available" : "unavailable" %>"><%= link_to copy_path(copy) do %><%= book_cover_tag(copy.book) %><% end %></li><% end %>
</ul>
